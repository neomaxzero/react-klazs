{
  "name": "react-klazs",
  "version": "1.0.0",
  "author": "neomaxzero",
  "description": "Override CSS classes with ease",
  "main": "build/klazs-min.js",
  "peerDependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0"
  },
  "dependencies": {
    "babel": "^6.23.0",
    "lodash": "^4.17.5",
    "react": "16.2.0",
    "react-dom": "16.2.0",
    "react-scripts": "1.1.0"
  },
  "devDependencies": {
    "coveralls": "^3.0.0",
    "rollup": "^0.57.1",
    "rollup-plugin-uglify": "^3.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "rollup -c",
    "test": "react-scripts test --env=jsdom",
    "afterScript": "react-scripts test --env=jsdom --coverage && cat ./coverage/lcov.info | coveralls"
  }
}
